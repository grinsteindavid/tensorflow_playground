import { learnLinear } from './learn-linear';

describe('utils', () => {
    describe('learnLinear', () => {
        it('should return ', async () => {
            const result = await learnLinear([20]);
            expect(result).toBe(0);
        });
    });
});
